const express = require("express");
const router = express.Router();

router.get("/_api/v1/datacenter/nodes", async (req, res) => {});

router.post("/_api/v1/datacenter/nodes", async (req, res) => {});

module.exports = router;
