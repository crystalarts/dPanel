const express = require("express");
const router = express.Router();

router.get("/_api/v1/datacenter/dbserver", async (req, res) => {});

router.post("/_api/v1/datacenter/dbserver", async (req, res) => {});

module.exports = router;
